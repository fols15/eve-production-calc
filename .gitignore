node_modules/

# Downloaded Fuzzwork SDE CSV dumps — large, regenerated by `npm run build-data`
data/raw/

# Private/local user state: EVE SSO client secret, OAuth tokens, personal
# structure list — never commit these.
data/user/

# Runtime logs
server_out.log
server_err.log

# Stray local test artifact, not used by the app
tree_test.json
